#include<stdio.h>

int main(){
    
    float P = 200000;   // Principal Amount
    float I = 1.5;     // Interest rate
    float T = 2;      // Time period annual

    printf("value of simple interest is = %f", (P*I*T)/100);

    return 0;
}